# Версия закреплена той, на которой прогоняются тесты. Раньше здесь стояла
# 3.15.0, а проверялось всё на 3.30 — то есть чистая установка получала не
# тот aiogram, на котором код испытан, и расхождение вылезло бы на сервере.
aiogram==3.30.0

# Ниже версии НЕ закреплены намеренно: у этих пакетов есть нативные части, и
# готовая сборка существует не под каждую версию Python. Жёсткое закрепление
# на свежем интерпретаторе означает сборку из исходников — долгую, требующую
# компилятора и Rust, и часто неудачную. Пусть pip берёт ту версию, под
# которую сборка есть.
aiohttp
python-dotenv==1.0.1
# [socks] — прокси для Fragment: без него socks5:// падает на импорте, и
# продавец видит «Missing dependencies for SOCKS support» вместо ответа.
requests[socks]
tonsdk
psycopg2-binary
redis
# Шифрование seed-фразы TON в хранилище. Приходил транзитивно, но
# держать защиту чужого кошелька на чужой зависимости нельзя.
cryptography
